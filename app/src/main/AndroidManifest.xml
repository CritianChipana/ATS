<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ats">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".Eliminar_esto"></activity>
        <activity android:name=".frame_11" />
        <activity android:name=".frame_5" />
        <activity android:name=".crear_cuenta" />
        <activity android:name=".frame_10" />
        <activity android:name=".confirmar_practica" />
        <activity android:name=".ResultadosSimulacion" />
        <activity android:name=".PreguntaSimulacro" />
        <activity android:name=".confirmarsimulacion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>